# hoox tests.
#
# Test suites are added as milestones land:
#   - compat unit tests           (M1)
#   - test harness sample         (M1.5)
#   - disasm differential tests   (M2)
#   - arch-x86 writer/relocator   (M4)
#   - interceptor tests           (M6)
#
# Nothing to build yet (M0).
